extends layout

include ../scripts/mixins

block layout-content
	script.
		function confirmSubmit() {
		}

	h1.MenuTitle #{title}

	div.Title
		form(action=actionPath onsubmit="return confirm('Are you sure you want to delete this asset?');" method="post")
			table(style='width: 50%, max-width 100px', class="middle")
				tr
					td.b ID:
					td
						input(type="text" id="assetID" name="assetID" required)
				tr
					td
						h4.label(for='PDCselection' id='PDCselect') PDC:
					td
						select(name='pdc' id='PDCselection' required)
							option(value= '' disabled selected)
							each val, index in PDC
								option(value= PDC[index]) #{PDC[index]}
				tr
					td(style="height:25px")
				tr
					td(colspan="2" style="text-align:center")
						button(type="submit" class="SubmitButton" ) Submit

	
	+returnNavButtons
